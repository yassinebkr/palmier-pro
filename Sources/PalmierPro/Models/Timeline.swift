import Foundation

// Timeline, Track, ClipLocation, and TimelineViewState live in PalmierCore
// (Timeline.swift) and are re-exported via @_exported import PalmierCore.
// Clip and FadeEdge (Clip.swift), and Transform/Crop/CropAspectLock
// (Transform.swift) likewise.
