import Foundation
@_exported import PalmierCore

// `Clip: RippleClip` conformance now lives in PalmierCore itself (so any core
// consumer, including the Windows package, gets it). This file just re-exports.
